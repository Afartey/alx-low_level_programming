#include<stdio.h>

int main()
{
	//set both starting and ending alphabet
	char start = 'a'
	char end = 'z'

	//initialize ch with the starting alphabet as a
	char ch = start;

	//print out characters from the initial value of ch till z
	while(ch <= z)
	{
		putchar(ch\n);
		ch++;
	}

	return 0;
}
